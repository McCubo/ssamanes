{{ form_start(form) }}
    {{ form_errors(form)  }}
    <div class="form-group">
    {{ form_label(form.name) }}
    {{ form_widget(form.name) }}
    {{ form_errors(form.name) }}    
    </div>
    <div class="form-check">
    {{ form_widget(form.status) }}
    {{ form_label(form.status) }}
    {{ form_errors(form.status) }}
    <small class="form-text text-muted">Los registros inactivos no seran elegibles en la lista lugares de trabajo.</small>
    </div>
    <br />
    <button class="btn btn-primary"><i class="fa fa-save"></i> {{ button_label|default('Guardar') }}</button>
    <a href="{{ path('work_place_index') }}">
    	<button class="btn btn-danger" type="button"><i class="fa fa-arrow-circle-left"></i> Volver a la lista principal</button>
    </a>
{{ form_end(form) }}