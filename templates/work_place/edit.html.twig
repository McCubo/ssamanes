{% extends 'blank.html.twig' %}

{% block body %}
    <h1>Edit WorkPlace</h1>

    {{ include('work_place/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('work_place_index') }}">back to list</a>

    {{ include('work_place/_delete_form.html.twig') }}
{% endblock %}